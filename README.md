# MeetingAgent Models

This repository stores the model package required by MeetingAgent v0.3.0.

The package is split into files below GitHub's per-file size limit. The installer downloads each part, verifies its SHA-256 checksum from `manifest.json`, joins the parts, verifies the archive, and extracts it automatically.

Do not download or combine the parts manually unless you are troubleshooting an installation.
